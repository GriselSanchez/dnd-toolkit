import { styled } from '../../../../stitches.config'
import { CardContainer } from '../../5eCard'

export const StyledCardContainer = styled(CardContainer, {
  width: '75vw',
  boxShadow: '0 0 5px #979aa4',
})

export { default as BaseCombo } from './BaseCombo'
